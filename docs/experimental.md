# ReScript Experimental Surface

- Live-provider concepts are modeled as explicit fail-fast in Tranche D.
- `--experimental-provider` exits non-zero with `rescript-stakeholder.experimental-provider-deferred`.
- OpenAI-compatible APIs, Anthropic APIs, and consumer-account adapters remain follow-on work.
- Experimental additions must not change seeded deterministic fixtures by default.
