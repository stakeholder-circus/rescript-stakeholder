# ReScript Traceability

- Rust source audit anchors: `rust-stakeholder/src/main.rs`, `src/activities.rs`, `src/domain.rs`, and `src/generators/common.rs` define CLI flags, scheduling, event envelope shape, family labels, and descriptors.
- Java audit anchors: `java-stakeholder/core/spec/parity-rules.md` and `core/spec/error-model.md` define strict flags, normalized parity, and fail-fast error categories.
- ReScript target: `rescript_src/Cli.res` implements Tranche D deterministic normalized JSON, dedicated classic-six plus modern-core, grouped fallback, and experimental-provider fail-fast.
